import { MongoClient, ObjectId } from "mongodb"

const mongoUrl = "mongodb://localhost:27017/"
const mongoDB = "backend" // database name

const client = new MongoClient(mongoUrl)

export default class backend {

    
}